{
  "name": "root",
  "private": true,
  "workspaces": [
    "plugins",
    "examples"
  ],
  "scripts": {
    "dev": "sh ./bin/dev.sh",
    "build": "npm run build:plugins && npm run build:examples",
    "build:plugins": "cd plugins && npm run build",
    "build:examples": "cd examples && npm run build",
    "docs": "cd plugins && npm run docs",
    "deploy": "sh deploy.sh",
    "test": "sh test.sh"
  }
}
